FPDF ~1.53 (or 1.51) goes here: fpdf.php + font/ directory.

The original production server kept it at /usr/local/htdocs/html/fpdf153/
(and fpdf151/); it was never part of the local html/ folder, so it could not
be vendored automatically.
